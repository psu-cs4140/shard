defmodule Shard.Repo.Migrations.CreateMudTables do
  use Ecto.Migration

  def change do
    # This migration is now split into two separate migrations
    # to ensure proper order of table creation
  end
end
